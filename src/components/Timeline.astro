---
interface Props {
    date: string;
    title: string;
    subtitle: string;
    descriptions: string[];
    current?: boolean;
}
const { date, title, subtitle, descriptions, current } = Astro.props;

const bg = current ? 'bg-blue-500' : 'bg-white';
const text = current ? 'text-blue-500' : 'text-white';
---

<li>
    <div class='flex-start flex items-center pt-3'>
        <div class={`-ml-[5px] mr-3 h-[9px] w-[9px] rounded-full ${bg}`}></div>
        <p class={`text-sm ${text}`}>
            {date}
        </p>
    </div>
    <div class='t-timeline-content mb-6 ml-4 mt-2'>
        <h2 class='t-timeline-head mb-1.5 text-2xl font-semibold text-white dark:text-white'>{title}</h2>
        <h3 class='t-timeline-head mb-1.5 text-xl font-semibold text-white dark:text-white'>{subtitle}</h3>
        <p class='mb-3'>
            {
                descriptions.map((description) => (
                    <span class='block mb-1.5'>{description}</span>
                ))
            }
        </p>
    </div>
</li>
