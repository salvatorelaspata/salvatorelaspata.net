---
import SectionLayout from '../layouts/SectionLayout.astro';
interface Props {
    bgColor: string;
    textColor: string;
}
const { bgColor, textColor } = Astro.props;
---

<SectionLayout bgColor={bgColor} textColor={textColor} title='Experiences' />
